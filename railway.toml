[build]
  builder = "python"
  buildCommand = "pip install -r requirements.txt"
  
[deploy]
  startCommand = "gunicorn app:app --bind 0.0.0.0:8080 --workers 2 --timeout 120"
  healthCheckPath = "/api/health"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
